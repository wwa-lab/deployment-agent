# Changelog

## Unreleased

- Polished **Resource Center** card and filter visuals after local review: link cards now carry a per-kind left accent bar (docs blue, tool teal, repo near-black, workspace purple) through a shared `--accent` custom property so link types read at a glance without touching the brand-icon whitelist, the primary blue is consolidated into `--rc-accent` design tokens replacing scattered `#2563eb` literals in the search focus ring, kind chips, and sidebar nav, group blocks gain a hairline divider, card titles bump to 14px, and the open-arrow affordance is now persistently visible at low opacity and brightens on hover; the Vite dev proxy target was also corrected from `:8080` to `:8081` to match the `local` profile `server.port`.

- Differentiated **Resource Center** SDLC stage visuals: stage number badges in group headers and the sidebar nav now follow a cool-hue ramp from Planning blue to Maintenance teal-green so the seven stages read as a forward progression, and links whose `kindLabel` is `Feedback` carry a small teal pen badge on their brand icon so write-back entries are visually distinct from plain guideline documents at a glance. Icon whitelist, seed data, and catalog contracts are unchanged.
- Refined **Resource Center** card readability and pending-state visuals: link card titles now clamp at two lines instead of truncating mid-name on one line, the card grid minimum column width is roomier (260px) so groups fill rows better, pending-URL cards drop the repeated "Pending" text flag in favor of a dashed border plus a single amber status dot (tooltip/ARIA label retained), and workspace/agent cards get a subtle purple tint with matching hover state so they are distinguishable from plain document/tool links at a glance.
- Polished **Resource Center** card/nav details after the denser layout: muted gray pending flags (no orange pills), removed redundant kind labels on cards, stripped repeated scope prefixes from sidebar/group titles (e.g. `Platform` instead of `Common · Platform`), two-line description clamp, roomier card padding, and removed the duplicate sticky page title so only the workspace topbar shows "Resource Center" while the in-page strip keeps search/actions. Brand `iconKey` assets remain unchanged.
- Restructured the **Resource Center** page into a denser tool layout: sticky single-line toolbar with centered `/` search, compact Recent chips (hidden when empty), sticky left section nav with scroll-spy, de-glassed group headers, and tighter link cards so the first viewport shows real destinations instead of hero/filter chrome. Brand `iconKey` assets and whitelist are unchanged.
- Added **Resource Center** (formerly Service Directory) as a Platform-shared capability at `/wwa/resource-center`: a config-driven catalog of SDLC stages, common platforms, and external destinations with scope/kind/stage/text filters, browser-local Recently used (max 8, key `wwa.resourceCenter.recent.v1`), guest read access, and `DEVOPS_ADMIN` manage mode with optimistic concurrency (`expectedVersion` on updates/deletes), dedicated audit actions (`resource_center_update` / `resource_center_delete`), and a separate `DA_SERVICE_DIRECTORY_CATALOG` store (Flyway `V20`) that never touches Configuration Management. Flyout and Home Shared Controls share one registry entry (`resource-center`); `/wwa/service-directory` redirects to the new route for bookmarks. Optional per-link `iconKey` whitelist (`confluence`, `jira`, `github`, `jenkins`, `ansible`, `arcad`, `peoplesoft`, `learning`, `infosec`, `vendor`, `wwa`) renders local official brand logos with letter-badge fallback; admins can pick icons in manage mode. ARCAD and GitHub Enterprise seed links remain "URL pending" until production URLs are supplied (SD-T02).
- Updated visible branding and UI shell copy to present the repository as WWA-Atlas Engineering Delivery Hub / WWA-Atlas Hub while preserving Deployment Agent as the M6 Deployment agent and retaining existing `/wwa/*`, `/wwa/deployment-agent`, `/api/deployment-agent`, Maven artifact, and Java package compatibility identifiers.
- Repositioned the open-collaboration package as two related competition entries: Atlas Engineering Delivery Hub as the parent Framework / team delivery framework, and Atlas Engineering Delivery Hub - Deployment as the independent M6 Tool / Function entry, including sanitized IBM iSeries one-click release UTL design direction, separate Deployment submission docs, and updated Hub/Deployment SDD traceability.
- Packaged the repository for the internal open collaboration competition as the Atlas Engineering Delivery Hub Framework entry: refreshed the root README, added a Chinese README, contribution guide, submission and pitch docs, framework documentation index, SDD packaging chain, Mermaid/SVG diagrams, and a synthetic adoption sample while preserving the previous WWA Agent Workspace Hub README as a reference baseline.
- Added an Agent Contribute Dashboard under WWA shared controls that maps the Qilianshan SDLC into an end-to-end coverage map plus a filterable coverage-matrix dashboard across seven SDLC stages, distinguishes implemented stages from in-progress Testing and not-yet-implemented Discovery and Maintenance, lets DEVOPS_ADMIN update stage status, including Backlog, through a persisted platform API, links each selected stage to Confluence guideline and feedback entry points, shows each agent-owned contribution item with Agent Owner, Sub-agent Owner, Process Owner, Technical Leader, Co-Build, and contribution notes without turning the page into an agent registration flow, and backfills the full SDD artifact chain for future documents-first iteration.
- Fixed AUTO execution monitor persistence so enabling background Jenkins/Ansible polling no longer crashes on oversized remote status text. The monitor now truncates polled status messages and external URLs to the database column limits before saving `DA_TASK_EXECUTION_HISTORY`, which prevents Oracle errors such as `EXTERNAL_STATUS_MESSAGE` exceeding `VARCHAR2(2000)` when an external API exception includes a full HTML error page or other verbose response body.
- Renamed the internal Java package namespace and application/build metadata from `deployment-agent` to `agenthub` so the backend source tree, Spring application name, Maven artifact, and frontend package naming now align with the current internal project identity. This is a technical-identifier migration only: existing user-facing workspace and API routes such as `/wwa/deployment-agent` and `/api/deployment-agent` remain unchanged for now.
- Expanded Build Agent task docs from read-only guidance into editable task metadata: the `Docs` dialog now still pre-fills our recommended `Primary Skill`, `Input Docs`, and `Output Docs`, but Build Agent users can adjust the actual markdown links per task, add or remove links, reset back to the recommended set, and save those overrides into task-level `customFields` so the docs shown in the UI can follow each project's real GitHub markdown structure without affecting other agents.
- Changed the Build Agent downloadable Excel template so it now ships as a common IBM i skill starter pack instead of a single legacy Maven-style example row: the template includes the full fixed set of common build-agent skills as prefilled rows, puts `ibm-i-workflow-orchestrator` first, orders the remaining rows according to the orchestrated workflow path, and gives each row `Execution Type = MANUAL`, `Script to be executed = <skill name>`, a reusable JSON `Parameter (input)` skeleton (`runner`, `project`, `workItemId`, `stage`, `inputDocs`, `outputDocs`, `notes`), and a more explicit expected-output description so users can delete what they do not need and make only small project-specific edits before upload.
- Added lightweight task-level onboarding docs for Build Agent without changing the backend data model: Build tasks can now expose a `Primary Skill` plus multiple `Input Docs` and `Output Docs` sourced from a frontend-only catalog of GitHub markdown links, the shared detail view now shows a `Skill` column and a `Docs` action when a Build task has mapped guidance, and both the new read-only docs dialog and the existing task edit dialog surface those links so newer operators can quickly understand what each task consumes and produces.
- Fixed the upload dialog UX for multi-stage workspaces such as Deployment Agent: the `Upload` button no longer appears to do nothing when a required field is missing, and instead shows a clear validation message and scrolls back to the missing field (most notably the required Stage selector when the user has scrolled down to the file picker). The file picker now also nudges users toward real `.xlsx` uploads instead of accepting legacy `.xls` selections.
- Made upload failures much more visible in the dialog itself: validation and backend import errors now surface next to the footer action buttons instead of only at the top of the scrollable modal, and API errors now include backend `details` text when present so cases like uploading an empty template explain the actual problem immediately.
- Extended Configuration Management's `Scope Directory` to support optional `Agent` overrides alongside `Application` and `SNOW Group`. Admins can now create, edit, filter, and view agent-scoped directory entries, `scopeSource` reports `Agent Override` when that third scope level is present, and upload dialogs only surface generic directory rows plus rows that match the active workspace agent.
- Changed blank-identifier upload semantics for Deployment-style rollouts to avoid accidental stage overwrite: when users leave `Workflow Identifier` empty, each upload now creates a fresh rollout instead of implicitly attaching a later-stage upload to the newest matching project. To stitch SIT/UAT/PROD into one overall summary row and still support multiple attempts per stage, users now reuse the same explicit `Workflow Identifier` across those uploads.
- Refined the stitched release summary visuals for Deployment-style stage tracking: `Current Stage` badges now use a progressive SIT → UAT → PROD color ramp, stage columns inherit stage-specific tones even when the underlying status is the same (for example three separate `Pending` chips no longer look identical), and `Overall Status` now uses explicit status-based colors so `Pending`, `Running`, `Completed`, `Failed`, and related states are easier to distinguish at a glance.
- Fixed Template Management's agent picker so it no longer surfaces application names such as `PowerCARD` as if they were real agents. The create/edit dialog now only offers enabled agents from the shared registry, and the local PowerCARD production mock template is correctly classified under `Deployment Agent` with `application = PowerCARD`.
- Improved upload error handling for malformed Excel files: when a user uploads a file that is not a valid `.xlsx` workbook, the backend now returns a friendly validation message telling them to upload a real `.xlsx` file and prefer the downloaded template, instead of surfacing a raw Apache POI stack trace.
- Added anonymous guest mode so anyone can preview the platform without a Team Book account or any setup: new `POST /api/platform/auth/guest` endpoint mints a session-backed `UserContext` with a dedicated `GUEST` role, guest users can browse every page because `UserContext.hasScopedAccess()` returns `true` for guest viewers (bypassing application/SNOW-group scope filters for reads only), and a new `GuestReadOnlyFilter` sits after the auth filters and blocks every non-`GET/HEAD/OPTIONS` request from a guest session with `403 Forbidden` (the only POST exception is `/api/platform/auth/logout`, so guests can end their own session). The login page now has a `Continue as Guest (read-only)` button alongside the normal sign-in form, and the workspace top bar shows a clear `Read-only preview` badge plus an `Exit` action instead of `Logout` while in guest mode. No audit or write path is affected for real users — guest permissions are minimal (`platform.enter`, `release.view`, `audit.view`, `platform.audit.view`), and the filter-based read-only enforcement is independent of UI state so hand-crafted requests are blocked too. `mvn test` passes 414 / 414 and `frontend && npm run build` is green.
- Reserved the per-agent upload-template customization seam (architecture.md §MVP Foundation Seams #8) so that all three agents continue to share one default XLSX template today while leaving a cheap path for a future customer to add columns on one agent without touching the shared `DA_TASK` schema or the other agents' templates: introduced a `TemplateSchema` record + Spring-managed `TemplateSchemaRegistry` keyed by `AgentId` with a single `DEFAULT_SCHEMA` registered for all three agents, reworked `UploadTemplateService` to read headers / sheet name / sample row / downloadable file name from the registry (preserving the no-arg `generateTemplate()` overload for existing tests and the legacy `/api/platform/upload/template` fallback route), added per-agent template download routes under each agent's existing upload controller (`/api/deployment-agent/upload/template`, `/api/testing-agent/upload/template`, `/api/build-agent/upload/template`) that each force their own `AgentId` server-side, pointed each frontend agent's `downloadTemplate` function at its own new route, added a `custom_fields` CLOB column to `DA_TASK` via `V18__add_task_custom_fields.sql` plus a matching `Task.customFields` JSON field destined to receive values from any schema that later declares extra columns, and derived `ExcelParserService.SHEET_NAME` from the default schema so the generator and parser cannot silently drift. Zero runtime behavior change in MVP: the three per-agent template endpoints return byte-identical files, `custom_fields` stays `NULL` for every task, and `mvn test` is green at 414 / 414.
- Made the task decision endpoint idempotent for terminal states: double-clicking Approve / Reject / Skip on a task that is already in the resulting state now returns 200 as a successful no-op instead of producing a confusing `InvalidStateTransitionException` / 422 response, without writing a duplicate audit entry or publishing a duplicate domain event. `Rerun` remains deliberately non-idempotent because it must create a fresh execution history attempt every time it is invoked.
- Added a transactional outbox seam for domain events so the future email notification dispatcher (and any other downstream integrations — webhook, Slack, BI) can be built as pure additive code instead of a cross-cutting retrofit: new `DomainEventPublisher` interface, `OutboxDomainEventPublisher` implementation that writes to a new `DA_OUTBOX_EVENT` table inside the caller's business transaction (so event persistence is atomic with the state change that produced it), and a first integration point in `DecisionEngine` that publishes a `task.<decision>` event for every successful, non-noop decision. MVP has no consumer — rows accumulate in `PENDING` state and nothing reads them. The canonical Oracle DDL is in `V17__add_outbox_event_table.sql`, and `docs/04-architecture/architecture.md` §"MVP Foundation Seams" documents the seam, its rules, and what it explicitly does not provide.
- Closed the three day-1 infrastructure gaps identified in the architecture review: adopted Flyway as the schema migration authority on the `dev` (Oracle) profile while keeping `local` / `test` on Hibernate DDL auto for fast iteration (renaming the duplicate `V3__add_execution_sync_columns.sql` to `V3.1` to resolve a version conflict that would have blocked any future migration tooling), introduced a request-scoped `CorrelationIdFilter` that stamps every inbound request with an `X-Correlation-Id`, threads it through SLF4J `MDC`, the logback pattern, a new `correlation_id` column on `DA_AUDIT_LOG_ENTRY`, and a shared axios request/response interceptor so user-facing error toasts can surface a reference the operator can grep, and introduced a `Clock` bean that the six services writing durable entity timestamps (`TaskExecutionHistoryService`, `RecordResultService`, `AutoExecutionService`, `ExternalExecutionMonitorService`, `ReleaseFlowService`, `AccessGrantService`) now use instead of calling `Instant.now()` directly. Every change is active in MVP (not a seam), documented in `docs/04-architecture/architecture.md` §"Infrastructure Foundations", and preserves full behavior: `mvn test` stays green at 411 / 411, the frontend still builds, and no existing test needed to change.
- Added a curated `Scope Directory` to Configuration Management so DEVOPS_ADMIN users can maintain reusable `Application` and `SNOW Group` choices in one place, and updated upload dialogs to choose those values from dropdowns while auto-assigning `Agent` from the active workspace instead of asking uploaders to type it.
- Laid the MVP foundation seams for the future "7×24 platform availability with human-in-the-loop decisions" workflow: added `ActorKind` (HUMAN / POLICY / AI_ASSISTED / SYSTEM) and `RiskLevel` (L0–L3) enums, `actor_kind` / `actor_ref` columns on the audit and task execution history tables, `risk_level` / `expected_sla_minutes` columns on tasks, a `DecisionGate` interface + `ManualDecisionGate` implementation that `DecisionEngine` now routes through, and reserved `supportsCancel()` / `cancel()` default methods on `AutoExecutionAdapter`. Every seam is zero-behavior in MVP (every write is `HUMAN`, every task defaults to `L2`, the gate always proceeds as human, and no runtime code calls `cancel()`), and the canonical Oracle DDL is in `V15__add_mvp_foundation_seams.sql`. The intent, rules, and non-goals for these seams are documented in `docs/04-architecture/architecture.md` §"MVP Foundation Seams", and `CLAUDE.md` now carries a red-line list of decisions that must never be auto-approved by policy, AI, or SLA timeout.
- Reserved an "AI Assist (Preview)" placeholder slot in the shared Decision and Task Edit dialogs so the frontend now has the wiring and UX anchor for a future AI advisor without any real model calls, backend work, or impact on existing human-in-the-loop decisions; the panel is gated by the `AI_ASSIST_PREVIEW_ENABLED` flag (`VITE_AI_ASSIST_PREVIEW=false` to hide) and its buttons are intentionally disabled so users cannot mistake the preview for live AI behavior.
- Fixed the WWA flyout workspace menu so it now clamps within the viewport and scrolls when necessary, keeping top entries like `Deployment Agent` and `WWA Home` reachable on shorter browser windows.
- Completed the Build Agent frontend so the workspace now supports real DEV-stage upload, list/detail navigation, and task-level edit/run/result/activity/decision actions instead of the earlier read-only generic wrapper; also aligned WWA home copy and repo/UAT/build-agent docs with the current `/api/platform/*` route split, shared template download contract (`/api/platform/upload/template`, `request-template.xlsx`), and frontend agent constants.
- Tightened the `.claude` implementation skills so `tasks-to-implementation` no longer silently chooses migration cutover strategy, treats new-project repos that call existing services as greenfield integrations instead of brownfield by default, scopes red-test baselines to relevant failures, clarifies when temporary infrastructure adapters are allowed, and `tasks-to-code` now clearly reserves itself for incremental brownfield work instead of overlapping with scaffolding or migration flows.
- Fixed Testing Agent detail dialogs that were still falling back to Deployment Agent API modules under the hood: the rundown edit dialog and task activity dialog now accept injected agent-specific API functions, and the Testing Agent detail page wires them to the `/api/testing-agent/...` clients so those actions stay inside the correct workspace boundary.
- Fixed Testing Agent task input handling around Ansible submissions: partial task-input edits now preserve previously uploaded fields such as `parameters` and `system`, and the Ansible adapter now keeps structured `parameters` payloads as structured `extra_vars` instead of flattening everything through `toString()`.
- Synced the task feedback behavior across shared workflow surfaces: the shared task edit/decision dialogs can now call the correct agent-specific APIs, Deployment Agent and Testing Agent detail pages now expose the same status-aware edit/decision options, and backend decision rules now allow `Skip` from `Awaiting_Review` with test coverage.
- Updated the WWA frontend copy so Testing Agent no longer inherits deployment-first wording in its primary flow: login now lands on the shared WWA home, Testing Agent actions and upload states use workflow/rundown language instead of `Start Deployment` / `Release File`, template-created rundowns route back to the matching agent workspace, and shared home/audit/access/config pages now use more neutral cross-agent descriptions.
- Split Oracle datasource settings out of the default Spring config into a dedicated `test` profile so `local` H2 startup no longer risks inheriting Oracle dialect settings; updated README and Docker docs to use the `test` profile for Oracle-backed runs.
- Added `DOCKER.md` with backend/frontend image naming plus copy-ready Docker `build`, `push`, `pull`, and `run` commands for local H2 and Oracle-backed startup flows.
- Corrected repo-operating documentation so agent/runbook guidance matches the current Spring Boot + Vue codebase: `AGENTS.md` and `CLAUDE.md` now use real commands and paths, the Claude SDLC playbook now points to the staged `docs/02..06-*` artifact locations and includes the repo-docs consistency review skill, and the UAT runbook no longer contradicts itself about the bundled Oracle baseline schema script.
- Cleaned the next layer of stale repository docs by fixing onboarding/review/migration references that no longer matched the repo, marking historical review artifacts as dated snapshots, removing nonexistent taxonomy/file references, and updating several review findings whose underlying code gaps have already been addressed.
- Closed the last two docs-vs-code review gaps by resolving the remaining `spec.md` field-source `TBD`s to match the implemented release-flow behavior and marking the early shared-menu HTML as a historical Deployment Agent prototype instead of a current UI baseline.
- Realigned the requirements/spec/design/architecture docs with the current codebase: task-level edit/run/result/decision actions are now documented as `task owner or DEVOPS_ADMIN`, task execution is described as explicit `Run` / `Record Result` / AUTO submit rather than auto-start, access-governance docs now reflect the real directory-search/manual-fallback behavior, and auth wording now makes it clear that Deployment Agent currently owns product authorization while real Team Book integration remains a future option.
- Refreshed `README.md` to match the current Deployment Agent codebase and latest design baseline, including the WWA workspace positioning, deny-by-default access model, stitched release-flow behavior, real local startup commands (`mvn` plus `frontend/npm`), current template-management limitations, and the Oracle-vs-`local` profile split.
- Enabled Template Management `Create Rundown` so eligible users can now launch a real release rundown from a template, review/edit the default scope in a create dialog, and land directly in the newly created rundown instead of seeing a permanently disabled placeholder button; the dialog now requires a release identifier in `xxx-sit-01 / xxx-uat-01 / xxx-prod-01` format and enforces stage alignment in both frontend and backend validation.
- Fixed Configuration Management component actions so built-in platform-default rows can now be opened in the edit dialog even before they are first persisted, added visible `Delete` actions for component rows, and backed scoped component deletion with a new admin API while keeping unsaved built-in defaults protected from deletion.
- Polished frontend UI copy across Configuration Management, Login, Release Summary, and Access Management by clarifying status-filter wording, fixing capitalization and phrasing (`Team Book`, `Human-in-the-Loop`, `SNOW Group`), and expanding abbreviated role labels for clearer page text.
- Changed Configuration Management so entering the page now lands on the `Component` tab by default instead of opening the derived `Configuration` tab first.
- Refreshed the WWA frontend background treatment with a lighter gradient-driven shell, upgraded glassy surface tokens, and more atmospheric login/home page styling so the workspace feels less flat while keeping dense business pages readable.
- Clarified WWA intro copy across login and workspace landing pages so the product is positioned accurately as DevOps automation today, with AI-assisted capabilities described as a future direction rather than a current-state promise.
- Pushed the WWA shell further toward a control-center look by adding grid-backed console panels, stronger operational hero treatments, and colder status-driven visual cues across the login experience, home page, and workspace chrome.
- Fixed stitched release-family grouping for infix stage release identifiers (`<name>-sit-01`, `<name>-sit-02`, `<name>-uat-01`, `<name>-prod-01`), so repeated SIT uploads and later UAT/PROD uploads are now connected in one rollout chain instead of appearing as separate families.
- Fixed `Run Task` dialog behavior so manual tasks can proceed with pre-filled script/parameters from Excel without forcing users to edit fields first; in run mode the primary action now allows direct run with unchanged inputs.
- Fixed `Create New Template` dropdown defaults so Category / Agent / SNOW Group / Application / Site now auto-select a valid option when the dialog opens and re-align automatically if option lists refresh later, preventing blank-looking required selects.
- Fixed Template Management option sources so deleting all available templates no longer empties the Create Template dropdown lists; stable baseline options now remain available for Category / Agent / SNOW Group / Application / Site.
- Fixed Access Management `Add User` failures where Team Book search could find an employee but create still returned `Unknown employee ID`: backend create now falls back to directory exact-match lookup when direct ID lookup is unavailable, and grant creation now also supports manual `Staff ID + Name` entry when Team Book cannot resolve the employee.
- Fixed Access Management `Edit/Reactivate` save behavior so updated scoped visibility (`scopeGrants`) is now correctly sent to the API instead of being dropped client-side.
- Fixed Template Management row `More` dropdown behavior so action items are no longer clipped at the bottom of the table card, and the menu now dismisses when users click outside or press `Esc`.
- Allowed repeated uploads for the same stage under the same release identifier by creating incremented stage attempts (`attempt #`), exposed attempt numbers in release-flow detail responses, and added summary attempt-view modes (`latest` vs `history`) so rollout overviews can focus on current progress or include historical outcomes.
- Stitched release-family summary/detail views now keep stage-prefixed uploads such as `sit-01 / uat-01 / prod-01` connected in one overview, add an explicit `Current Stage` indicator plus missing-stage placeholders on the summary page, and teach upload-time release matching to treat those stage-prefixed identifiers as the same rollout family.
- Fixed Configuration Management first-load failures by making the configuration read APIs stop auto-bootstrapping built-in component rows into the database; missing platform defaults are now synthesized in-memory for display and only persisted when an admin actually saves changes.
- Added an `Add User` flow to Access Management so admins can search Team Book for directory users who do not yet have a WWA access grant, select a first-time employee, and create the grant from the same dialog without relying on the existing-grants list alone.
- Renamed the workspace across repository documentation from `Deployment Agent` to `Release Agent` so the product name better matches the current positioning around release orchestration, review control, and execution coordination; current technical identifiers such as `deployment-agent`, `/wwa/deployment-agent`, and `/api/deployment-agent` remain unchanged for now.
- Reworked Configuration Management so the Component tab now persists real scoped component metadata and drives the raw Configuration table from the same source; runtime config resolution now falls back from Agent Override to SNOW Group Default to Application Default to Platform Default, while built-in Jenkins/Ansible/callback credentials are encrypted at rest and no longer written to audit logs in plaintext.
- Added a recommended upload-time `Release Identifier` field so users can explicitly group repeated SIT/UAT/PROD uploads into the intended rundown, while duplicate same-stage uploads under the same identifier are now blocked with a clear validation error.
- Fixed upload import so re-uploading the same project/stage creates a new release rundown instead of overwriting the existing active one; later-stage uploads now attach to the newest eligible release flow that does not already contain that stage.
- Fixed the logged-in WWA top-bar title so agent workspace pages no longer repeat the same label as `Workspace › Page` when both values are identical.
- Added a persistent `WWA Home` return action in the logged-in workspace top bar, so users can jump back to the WWA home page without reopening the left-side flyout.
- Added a first-screen explanation that expands WWA to "Work With Agent" on the login page, the WWA home page, and the default Release Agent landing page.
- Removed placeholder application entries from the WWA sidebar so the shell only shows real workspace navigation.
- Added working Excel template download support in the upload dialog.
- Restored local-profile authentication filters so session-backed user context is available during frontend testing.
- Added a Rundown Information panel on the release-flow detail page for each stage tab.
- Added editable stage-level rundown fields and API support for SNOW group, application, site, and estimated remaining time.
- Added request-level Refresh, Start Deployment, and Mark as Failed actions to the release-flow detail rundown panel.
- Expanded Execution Mix to show both task counts and manual/auto percentages.
- Exposed task-level category from imported Activity category and displayed it in the release detail table.
- Reworked deployment task actions so Run and Rerun are always visible with state-based disabling, while rerun is no longer mixed into the regular review decision dropdown.
- Surfaced task dependency relationships in both template management and release detail views, including Blocked By / Blocks visibility and multi-select dependency maintenance when editing template tasks.
- Extended release upload access to DEVOPS_ADMIN and aligned the frontend upload entry state with the backend permission model.
- Replaced rundown hard-delete with Archive/Restore flow support, hiding archived release data from default views while letting DEVOPS_ADMIN inspect and recover archived rundowns.
- Added a DEVOPS_ADMIN-only `Delete Permanently` action for already archived rundowns, so admins can irreversibly purge archived task history without turning normal rundown cleanup back into hard delete.
- Added Phase 1 Access Management user stories to the product requirements doc, covering access grants, deny-by-default product entry, the admin console, permission enforcement, and access-governance auditability.
- Expanded `docs/03-spec/spec.md` with Phase 1 Access Management requirements, including Access Grant data, deny-by-default entry rules, Access Management functional scope, permission enforcement, and related risks/open questions.
- Updated the `docs/04-architecture` document set to reflect the same Phase 1 Access Management direction, including access-grant architecture, authorization data flows, and the planned data-model extension.
- Added a first visible multi-scope runtime slice for `Application / SNOW Group / Agent`: upload now captures scope context, rundowns persist an editable `agent`, release summary supports scope filters plus scope display, and release detail shows the active rundown scope more clearly.
- Extended the same `Application / SNOW Group / Agent` scope UX into Template Management and Configuration Management, including filter-driven template defaults, clearer scope context in template details, an Agent column in the template list, and shared-default scope labels in configuration views.
- Extended Audit Log with first-class `Application / SNOW Group / Agent` scope fields, backend filtering, and scope-aware display so multi-team activity can be traced beyond operator-only search.
- Updated the `docs/05-design` document set so the detailed design and API guide now align with the current MVP plus Phase 1 Access Management, replacing outdated callback-era assumptions with local access-grant, archive lifecycle, and fire-and-forget execution design.
- Rewrote `docs/06-tasks/tasks.md` into a remaining-work implementation plan for Phase 1 Access Management, replacing the old Node/TypeORM/callback-era task list with backend, frontend, audit, rollout, and verification workstreams aligned to the current design baseline.
- Updated `docs/IMPLEMENTATION_PLAN.md` from an outdated MVP-complete report into a current baseline + Phase 1 execution plan, centered on Access Grants, deny-by-default entry, admin bootstrap, rollout sequencing, and verification gates.
- Realigned the spec, architecture, design, tasks, and implementation-plan docs with the current workspace state, including implemented Access Grants, the `role` + `roles[]` + `permissions[]` auth contract, current audit visibility, existing-grants-only Access Management search, and the lighter dependency summary near the task table.
- Realigned the user stories, spec, architecture, design, tasks, and implementation-plan docs again with the latest workspace code, including scoped access grants, `scopes[]` in auth/session, runtime `Application / SNOW Group / Agent` scope, audit scope fields, and rundown owner/admin-only control actions.
- Added a dedicated UAT runbook covering Oracle/database prerequisites, backend and frontend bring-up order, integrated smoke checks, current authentication mode, and known pre-UAT blockers such as the missing baseline schema script.
- Added a ready-to-run Oracle current-schema SQL script for fresh UAT environments, so tables can be created directly from IntelliJ IDEA before backend startup.
- Standardized bilingual requirement and user-story documents to English-only wording so repository comments and docs align with the team's official documentation language.
- Started Phase 1 Access Management coding by adding Access Grant persistence, role-to-permission resolution, deny-by-default auth/session enforcement, bootstrap grants for local/dev users, and a `roles[] + permissions[]` auth contract across backend and frontend session state.
- Added backend Access Management APIs for listing, creating, updating, suspending, and reactivating Access Grants, with audit events for each grant lifecycle action.
- Added an Access Management workspace in the frontend with admin grant lifecycle controls, locked navigation for non-admin users, and audit-log labels for access-governance actions.
- Extended Access Management from product-only grants into scoped visibility grants backed by `Application / SNOW Group`, added scope editing to the access dialog, returned `scopes` in auth/session responses, and enforced the same scope boundary across release-flow visibility and audit-log visibility.
- Added a first-class rundown owner field, defaulted new rundown ownership from a single imported task owner or the uploader, showed the owner in rundown details, and limited `Start Deployment` / `Mark as Failed` to the rundown owner or DEVOPS_ADMIN.
- Added `Rundown Owner` to the Release Agent summary list so users can see who controls each rundown before opening the detail page.
- Moved dependency summary metrics out of the top Rundown Information block and into a lighter Task Dependencies panel above the task table, so stage metadata stays focused on execution status while dependency troubleshooting stays near Blocked By / Blocks details.
- Realigned task-row actions with the prototype by always showing Edit, View Result, and a Decision dropdown, with state-based disabling.
- Added tooltips to disabled task actions so users can see whether an action is blocked by role or task status.
- Simplified task action permissions so only the task owner or a DEVOPS_ADMIN can edit input, record results, submit auto execution, or apply decisions.
- Moved MANUAL task result submission into the Edit dialog so task-row actions stay closer to the prototype.
- Wired View Result to execution history so external job links and stored execution output can be viewed from the task result modal.
- Reworked the frontend shell from a standalone Release Agent sidebar into a WWA workspace with second-level navigation for Release Agent, Template Management, Configuration Management, and Audit Log.
- Redesigned Template Management details to treat each template as a multi-task deployment blueprint, with task tables aligned to the current deployment task structure.
- Added a lightweight More menu for each release template row with Clone, Edit, and Delete as template-maintenance entry points.
- Added a Create New Template modal with Manual Entry and Upload Excel tabs, reusing the current Excel-template download capability and creating local template drafts for frontend preview.
- Added local task authoring for template drafts, including Add Task, Edit, Delete, dependency maintenance, and automatic handoff from Manual Entry into the first task-creation step.
- Added Edit Template support by reusing the template-creation form for metadata maintenance from both the details header and the template-row More menu.
- Added working Clone behavior for template rows so a new local draft can be created with copied metadata and task definitions.
- Added a real Delete Template confirmation flow that removes local template drafts from the list and safely resets the selection state.
- Simplified template selection by making table rows switch the shared Template Details workspace directly and removing the redundant View Details button.
- Redesigned Configuration Management into a sample-inspired component workspace for Jenkins, Ansible, and callback integrations, while preserving a Raw Configuration tab for key-level admin edits.
- Fixed frontend configuration API mapping so backend `configKey` and `configValue` fields are correctly normalized before rendering and saving.
- Clarified local-login validation so the page now tells users that any non-empty password works during local testing.
- Made shared-capability navigation visible in the WWA sidebar even for users without access, with page-level access guidance instead of hiding the menu entirely.
- Changed Configuration Management to read-only access for all signed-in users, while keeping edit actions restricted to DEVOPS_ADMIN.
- Reworked the Configuration tab into a filterable admin table with application, owning-group, config-item, and value columns, backed by the current fixed configuration catalog.
- Opened Audit Log for read-only access to all signed-in users and refreshed the page presentation so audit history is visible without elevated roles.
- Reshaped Audit Log to better match the log-style sample with action, result, action-by, time, input message, and output message columns, and redacted sensitive config-update values from audit responses.
- Simplified Audit Log again into an action-record view with User, Time, Type, and Detail columns, plus Staff Id search for faster tracing.
- Fixed the Audit Log API wiring so the shared audit page loads correctly for signed-in users, and clarified the page copy around platform traceability versus task-level activity.
- Added a task-level Activity dialog on the release-flow detail page so users can trace who did what on a task, when it happened, and the related input/output from audit and execution records.
- Redesigned the Release Agent summary table so SIT, UAT, and PROD stage statuses are visible at a glance without opening the flow detail page.
- Reworked the workspace shell into a clearer two-level navigation so WWA appears as a first-level menu and its capabilities expand as second-level items.
- Added first-level placeholder applications around WWA so the left navigation reads like a broader platform shell while only WWA opens a working second-level flyout on the right.
- Fixed the WWA flyout positioning so the second-level workspace menu is no longer clipped by the sidebar scroll container.
- Refined the WWA flyout interaction so it closes after choosing a workspace page or clicking outside, and removed the redundant mini WWA heading from the flyout panel.
- Added a first-class Critical (Y/N) task field, surfaced it in the release detail table, included it in the Excel import/template flow, and used it as a workflow gate so tasks marked critical must be reviewed before the next pending task is released.
- Extended Template Management task authoring and details to include the same Critical (Y/N) gate flag, so template tasks can define review-blocking steps before a rundown is created.
