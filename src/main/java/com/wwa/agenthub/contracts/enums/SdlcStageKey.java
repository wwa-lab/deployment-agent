package com.wwa.agenthub.contracts.enums;

/** SDLC lifecycle stage identifier for stage-type directory groups. */
public enum SdlcStageKey {
    planning,
    estimation,
    discovery,
    build,
    testing,
    deployment,
    maintenance
}
