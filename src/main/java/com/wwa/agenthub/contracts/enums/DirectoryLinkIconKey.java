package com.wwa.agenthub.contracts.enums;

/** Whitelist keys for Resource Center link card icons (local assets only). */
public enum DirectoryLinkIconKey {
    confluence,
    jira,
    github,
    jenkins,
    ansible,
    arcad,
    peoplesoft,
    learning,
    infosec,
    vendor,
    wwa
}
